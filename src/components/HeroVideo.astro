<section class="relative h-screen w-full overflow-hidden">
    <video
        class="absolute top-0 left-0 w-full h-full object-cover z-0"
        autoplay
        muted
        loop
        playsinline
        preload="auto"
        poster="fallback.jpg"
    >
        <source src="../public/Video.mp4" type="video/mp4" />
        <source src="../public/video.webm" type="video/webm" />
        Tu navegador no soporta videos HTML5.
    </video>

    <div class="hero-content">
        <h1>Bienvenidos a Vagar Vacaciones</h1>
        <p>Descubre una experiencia inolvidable</p>
    </div>
</section>

<style>
    .hero-content {
        position: relative;
        z-index: 1;
        color: white;
        text-align: center;
        padding: 20px;
    }

    .hero-content h1 {
        font-size: 3em;
        margin-top: 200px;
    }

    .hero-content p {
        font-size: 1.5em;
    }

    /* Ocultar video y mostrar imagen en móviles si es necesario */
    @media (max-width: 768px) {
        .hero-video {
            display: none;
        }

        .hero-section {
            background: url("fallback.jpg") center center / cover no-repeat;
        }
    }
</style>
